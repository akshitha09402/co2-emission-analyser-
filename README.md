# co2-emission-analyser-
A machine learning project that predicts vehicle CO₂ emissions using features like engine size, fuel type, and fuel consumption. Includes data preprocessing, model training (Linear Regression, Random Forest, XGBoost), and a simple Flask app for real-time predictions.
